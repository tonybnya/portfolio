services:
  - type: web
    runtime: static
    name: portfolio
    buildCommand: npm run predeploy
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
